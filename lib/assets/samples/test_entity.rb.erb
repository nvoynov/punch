require_relative "<%= @model.test_helper %>"
include <%= @model.namespace %>

describe <%= @model.const %> do
  # let(:entity) { <%= @model.const %>.new(<%= @model.keyword_params %>) }

  it {
    skip "design test for <%= @model.const %>"
  }
end
