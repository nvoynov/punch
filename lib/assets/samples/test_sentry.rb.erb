require_relative "<%= @model.test_helper %>"

describe <%= @model.const %> do
  # @todo provide valid and wrong samples here
  let(:proper) { [nil, -1, 0, 1, "", "string", Object.new] }
  let(:faulty) { [nil, -1, 0, 1, "", "string", Object.new] }

  it {
    proper.each{|arg| assert_equal arg, <%= @model.const %>.(arg) }
    faulty.each{|arg| assert_raises(ArgumentError) { <%= @model.const %>.(arg) }}
  }
end
