<% require 'punch/gadgets/erb_helper' -%>
<%= test_helper %>
include <%= @model.namespace %>

describe <%= @model.const %> do
  let(:entity) { <%= @model.const %>.new(<%= parameters_string %>) }

  it 'must do something' do
    assert entity
  end
end
